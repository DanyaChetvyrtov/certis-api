package ru.digitalhustle.certis.enums

enum class BudgetOptimizationReason {
    FIXED_PRESERVED,
    LOW_UTILIZATION_REDUCTION,
    OVERSPENDING_REALLOCATION,
    NEAR_LIMIT_REALLOCATION,
    RISK_UNCHANGED,
    NO_CHANGE,
}
