# Optional local overrides for Gradle database tasks.
# Gradle properties (-Pdb.* / gradle.properties) take precedence over DB_* environment variables.
db.host=localhost
db.port=5432
db.name=certis
db.user=postgres
db.password=root
db.schema=keeper
